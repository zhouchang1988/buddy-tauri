# README 致谢与 MIT 许可证设计

## 目标

在 README 中感谢 linux.do 社区，并将项目许可证从 ISC 统一调整为 MIT。

## 变更

- 在 README 的 License 章节前新增“致谢”章节，正文为“感谢 [linux.do](https://linux.do/) 社区的反馈与支持。”
- 将 README 的许可证标识从 ISC 改为 MIT。
- 新增 `LICENCE`，使用标准 MIT 许可证正文，版权行为 `Copyright (c) 2026 David Hu`。
- 将 `package.json` 的 `license` 字段从 `ISC` 改为 `MIT`，确保元数据与文档一致。

## 验证

检查最终 diff，确认链接、章节顺序、许可证正文与包元数据一致，且不修改无关文件。
