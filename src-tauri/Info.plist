<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Declared so macOS resolves the app's language from the system
	     preferences, letting AppKit localize native panels (e.g. the open
	     dialog's Open/Cancel buttons) even though the app ships no .lproj
	     resources of its own. Merged into the generated bundle Info.plist
	     by tauri-bundler. -->
	<key>CFBundleAllowMixedLocalizations</key>
	<true/>
	<key>CFBundleLocalizations</key>
	<array>
		<string>en</string>
		<string>zh-Hans</string>
		<string>zh-Hant</string>
	</array>
</dict>
</plist>
